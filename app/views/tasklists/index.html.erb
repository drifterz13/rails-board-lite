<div class="flex gap-3">
  <div id="tasklists" class="flex gap-3">
    <%= render @tasklists %>
  </div>
  <%= turbo_frame_tag dom_id(Tasklist.new) %>
  <%= link_to "Create List", new_tasklist_path, data: { turbo_frame: dom_id(Tasklist.new) }, class: "text-accent btn btn-link"  %>
</div>
